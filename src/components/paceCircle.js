import React from "react";
import { View } from "react-native";

const PaceCircle = () => {
  return(
    <View style={{width: 130,
      height: 130,
      marginHorizontal: 10,
      borderRadius: 100,
      borderWidth: 20,
      borderColor: 'yellow'}}></View>
  )
}

export default PaceCircle;
